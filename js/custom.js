(function ($) {

	//create custom functions


	//load function while document ready
	$(document).on('ready', function () {

	});
	//load function while window load completly
	$(window).on('load', function () {

	});
	// load functions while window scroll
	$(window).on('scroll', function () {
		
	});
})(jQuery);